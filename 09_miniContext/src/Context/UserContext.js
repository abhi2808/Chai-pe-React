import React from 'react'

const UserContext = React.createContext()

export default UserContext;
// wrap tags with this, 
// would make it a provider, 
// uske under saare components iske context ke through saari states ka access le sakte hai